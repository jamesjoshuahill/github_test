The 'git clone <repository_location> (<directory_name>)' command will:

1) make a new directory called the name of the repository, or directory_name if specified
2) change into the new directory
3) initialise git in the new directory
4) fetch a copy of the repository
5) merge the fetched repository into the new local repository
6) add the repository location as the default remote